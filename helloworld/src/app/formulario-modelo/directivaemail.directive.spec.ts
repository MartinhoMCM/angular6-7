import { DirectivaemailDirective } from './directivaemail.directive';

describe('DirectivaemailDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaemailDirective();
    expect(directive).toBeTruthy();
  });
});
